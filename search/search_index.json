{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"tower-guard-rs","text":"<p><code>tower-guard-rs</code> is application-layer security middleware for tower-based services, powered by the guard-core-rs detection engine. It is part of the Guard ecosystem. It works with any framework built on <code>tower::Service</code>, including axum (axum-guard-rs composes this crate), hyper, and warp.</p> <p>The crate holds framework glue only: every detection decision comes from the engine. <code>GuardLayer</code> is a working <code>tower::Layer</code> and <code>GuardService</code> a working <code>tower::Service</code> over <code>http::Request&lt;B&gt;</code>. The adapter is fail-secure: any failure to complete the security check answers <code>500</code>, never an uninspected passthrough.</p>"},{"location":"#ecosystem-position","title":"Ecosystem position","text":"<pre><code>guard-core (Python)           &lt;- Reference implementation, spec owner\n\u251c\u2500\u2500 guard-core-rs             &lt;- Rust engine: detection, preprocessing, semantics\n\u2502   \u251c\u2500\u2500 tower-guard-rs        &lt;- Adapter: tower middleware (this repo)\n\u2502   \u251c\u2500\u2500 axum-guard-rs         &lt;- Adapter: axum layer over tower-guard-rs\n\u2502   \u251c\u2500\u2500 actix-guard-rs        &lt;- Adapter: actix-web middleware\n\u2502   \u2514\u2500\u2500 rocket-guard-rs       &lt;- Adapter: rocket fairing and guards\n\u251c\u2500\u2500 guard-core-go             &lt;- Go port\n\u2514\u2500\u2500 guard-core-ts             &lt;- TypeScript port\n</code></pre> <p>The adapter translates native request content into engine inputs (path, query string, header values, buffered body), runs one engine call per request view, and translates a threat verdict into a native block response.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>cargo add tower-guard-rs\n</code></pre> <p>The published crate is <code>1.0.0</code> and depends on the published <code>guard-core-engine</code> <code>4.0.4</code>. Requires Rust 1.92 or later (edition 2024) and tower 0.5. See Installation for details.</p>"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>use tower::Layer;\n\nlet layer = tower_guard_rs::GuardLayer::new(tower_guard_rs::default_config());\n\n// Any `Service&lt;Request&lt;B&gt;&gt;` works; `axum::Router` is the usual one.\nlet service = layer.layer(my_service);\n</code></pre> <p>For axum, axum-guard-rs wraps this layer with <code>with_guard(config)</code>.</p>"},{"location":"#what-it-inspects","title":"What it inspects","text":"<p>One engine call per request view, mirroring the mapping used by the sibling adapters:</p> Request part Engine context Notes Path <code>url_path</code> Skipped for <code>/</code> Query string <code>query_param</code> Skipped when empty Header values <code>header</code> Skips <code>sec-*</code> and the negotiation/routing headers Body <code>request_body</code> Buffered first, capped <p>The HTTP method is not fed to the engine: the engine's <code>detect(content, context, config)</code> takes content plus a context, and the reference adapters do not scan the method either.</p>"},{"location":"#responses","title":"Responses","text":"Situation Status Body Engine flags a view <code>403 Forbidden</code> <code>{\"detail\":\"Suspicious activity detected\"}</code> Body exceeds the cap <code>413 Payload Too Large</code> <code>{\"detail\":\"Payload too large\"}</code> Body read error or engine panic <code>500 Internal Server Error</code> <code>{\"detail\":\"Security check failed\"}</code>"},{"location":"#where-to-go-next","title":"Where to go next","text":"<ul> <li>Installation for requirements and dependency setup</li> <li>API for the full public surface and behavior tables</li> <li>Examples for runnable applications</li> </ul>"},{"location":"api/","title":"API reference","text":"<p>The public surface of <code>tower_guard_rs</code> <code>1.0.0</code>. The full crate documentation is also in <code>src/lib.rs</code> (build it with <code>cargo doc --open</code>).</p>"},{"location":"api/#middleware","title":"Middleware","text":""},{"location":"api/#guardlayer","title":"<code>GuardLayer</code>","text":"<p>The tower layer entry point. It implements <code>tower::Layer</code> over <code>http::Request&lt;B&gt;</code>; wrap any <code>Service&lt;Request&lt;B&gt;&gt;</code> with <code>Layer::layer</code>:</p> <pre><code>use tower::Layer;\n\nlet layer = tower_guard_rs::GuardLayer::new(tower_guard_rs::default_config());\nlet service = layer.layer(my_service);\n</code></pre> <p>Constructors and builders:</p> Method Description <code>GuardLayer::new(config: DetectConfig)</code> Build a layer from an engine <code>DetectConfig</code>. The body buffering cap starts at <code>config.max_full_scan_bytes</code> <code>GuardLayer::with_defaults()</code> Build a layer with <code>default_config()</code> <code>.with_body_cap(body_cap: usize)</code> Replace the body buffering cap, in bytes. A body larger than the cap is rejected with <code>413</code>. A cap of <code>0</code> rejects every request that carries a non-empty body"},{"location":"api/#guardservice","title":"<code>GuardService</code>","text":"<p>The <code>tower::Service</code> produced by <code>GuardLayer</code>. It screens a <code>http::Request&lt;B&gt;</code> by running one engine call per request view and, on a clean pass, hands the inner service a rebuilt request whose body is byte-identical to what the client sent (bodies are buffered under the cap and rebuilt via <code>B: From&lt;Bytes&gt;</code>). The inner service must be <code>Clone + Send</code> (tower's standard <code>Buffer</code>-style sharing).</p>"},{"location":"api/#guardbody","title":"<code>GuardBody</code>","text":"<p>The response body type emitted by <code>GuardService</code>, nameable because it appears in the service's <code>Response</code> association:</p> <pre><code>pub enum GuardBody&lt;B&gt; {\n    Passthrough(B),\n    Generated(Full&lt;Bytes&gt;),\n}\n</code></pre> <p>Either the inner service's response body, forwarded untouched (<code>Passthrough</code>), or a Guard-generated JSON body for a short-circuited response (<code>403</code>, <code>413</code>, or <code>500</code>, <code>Generated</code>). It implements <code>http_body::Body</code>.</p>"},{"location":"api/#boxerror","title":"<code>BoxError</code>","text":"<p><code>Box&lt;dyn Error + Send + Sync&gt;</code>, the boxed error type used by <code>GuardBody</code>. Identical to <code>tower::BoxError</code>; redefined so the body type does not force a <code>tower</code> re-export onto downstream type signatures.</p>"},{"location":"api/#configuration","title":"Configuration","text":""},{"location":"api/#default_config","title":"<code>default_config()</code>","text":"<p>Returns the reference default <code>DetectConfig</code>:</p> Knob Value <code>max_content_length</code> <code>10_000</code> <code>max_full_scan_bytes</code> <code>262_144</code> <code>preserve_attack_patterns</code> <code>true</code> <code>semantic_threshold</code> <code>0.7</code> <code>threat_score_threshold</code> <code>1.0</code>"},{"location":"api/#detectconfig","title":"<code>DetectConfig</code>","text":"<p>Re-exported from <code>guard_core_engine::detect</code>. Fields:</p> Field Type Meaning <code>max_content_length</code> <code>usize</code> Semantic budget and truncation budget <code>max_full_scan_bytes</code> <code>usize</code> Preprocessor full-scan cap (also the default body cap) <code>preserve_attack_patterns</code> <code>bool</code> Keep attack patterns in the processed view <code>semantic_threshold</code> <code>f64</code> Semantic analysis threshold <code>threat_score_threshold</code> <code>f64</code> Threat score threshold for a verdict"},{"location":"api/#behavior","title":"Behavior","text":""},{"location":"api/#what-it-inspects","title":"What it inspects","text":"<p>One engine call per request view:</p> Request part Engine context Notes Path <code>url_path</code> Skipped for <code>/</code> Query string <code>query_param</code> Skipped when empty Header values <code>header</code> Skips <code>sec-*</code> and the negotiation/routing headers Body <code>request_body</code> Buffered first, capped <p>The HTTP method is not scanned.</p>"},{"location":"api/#responses","title":"Responses","text":"Situation Status Body Engine flags a view <code>403 Forbidden</code> <code>{\"detail\":\"Suspicious activity detected\"}</code> Body exceeds the cap <code>413 Payload Too Large</code> <code>{\"detail\":\"Payload too large\"}</code> Body read error or engine panic <code>500 Internal Server Error</code> <code>{\"detail\":\"Security check failed\"}</code> <p>The adapter is fail-secure: any failure to complete the security check answers <code>500</code>, never an uninspected passthrough. Engine panics are caught with <code>catch_unwind</code> (note that <code>panic = \"abort\"</code> in a release profile disables that recovery).</p>"},{"location":"api/#constants","title":"Constants","text":"<p>Re-exported refusal message bodies:</p> Constant Value <code>BLOCKED_MESSAGE</code> <code>\"Suspicious activity detected\"</code> <code>OVERSIZE_MESSAGE</code> <code>\"Payload too large\"</code> <code>FAILURE_MESSAGE</code> <code>\"Security check failed\"</code>"},{"location":"api/#engine-re-exports","title":"Engine re-exports","text":"<p><code>DetectConfig</code>, <code>DetectVerdict</code>, and <code>Threat</code> are re-exported from <code>guard_core_engine::detect</code>. A <code>DetectVerdict</code> carries <code>is_threat</code>, a <code>threat_score</code>, and the list of <code>Threat</code> findings (regex or semantic).</p>"},{"location":"examples/","title":"Examples","text":"<p>The repository ships two runnable applications under <code>examples/</code>. Both use the real adapter surface (<code>GuardLayer</code> wrapped around a service).</p> <p>The example crates are workspace members and build against the in-repository path dependency, so building them locally requires a sibling <code>guard-core-rs</code> checkout (see the repository README).</p>"},{"location":"examples/#simple_app","title":"simple_app","text":"<p>A minimal guarded service served over hyper (<code>examples/simple_app</code>):</p> Route Guard Behavior <code>GET /health</code> excluded <code>200 ok</code>, served before the guard <code>GET /</code> guarded <code>200</code> greeting <code>GET /search?q=...</code> guarded <code>200</code>, or <code>403</code> when the query trips the engine <code>POST /echo</code> guarded echoes the body, or <code>403</code>/<code>413</code> from the guard <p>The <code>/health</code> branch runs before the guard, mirroring the excluded-path behavior of the Python distro's pipeline: the adapter itself scans every request it sees, so exclusion is a routing decision, not a guard option.</p> <p>Run it:</p> <pre><code>cargo run -p tower-guard-simple-app\n</code></pre>"},{"location":"examples/#advanced_app","title":"advanced_app","text":"<p>A production-shaped guarded service (<code>examples/advanced_app</code>) that demonstrates the two knobs a real deployment tunes: environment-driven engine configuration and route-scoped guard configuration. <code>/admin/*</code> traffic is screened by a second, stricter <code>GuardLayer</code> (threat-score threshold halved by default), while general routes use the default-derived configuration, and <code>GET /health</code> is answered in front of both guards.</p>"},{"location":"examples/#configuration","title":"Configuration","text":"Variable Meaning Default <code>APP_ADDR</code> Listen address <code>0.0.0.0:8080</code> <code>GUARD_MAX_CONTENT_LENGTH</code> Engine <code>max_content_length</code> <code>10000</code> <code>GUARD_MAX_FULL_SCAN_BYTES</code> Engine <code>max_full_scan_bytes</code> (also the default body cap) <code>262144</code> <code>GUARD_PRESERVE_ATTACK_PATTERNS</code> Engine <code>preserve_attack_patterns</code> <code>true</code> <code>GUARD_SEMANTIC_THRESHOLD</code> Engine <code>semantic_threshold</code> <code>0.7</code> <code>GUARD_THREAT_SCORE_THRESHOLD</code> Engine <code>threat_score_threshold</code> (general routes) <code>1.0</code> <code>GUARD_BODY_CAP</code> Adapter body buffering cap <code>GUARD_MAX_FULL_SCAN_BYTES</code> <code>GUARD_ADMIN_THREAT_SCORE_THRESHOLD</code> Threat-score threshold for the <code>/admin</code> guard tree half the general threshold"},{"location":"examples/#routes","title":"Routes","text":"Route Guard tree Behavior <code>GET /health</code> excluded <code>200 ok</code> <code>GET /</code> general <code>200</code>, greeting text <code>GET /search?q=...</code> general <code>200</code>, or <code>403</code> on a threat <code>POST /echo</code> general echoes the body; <code>403</code>/<code>413</code> from the guard <code>GET /admin/stats</code> stricter admin guard screened by a second <code>GuardLayer</code> with a lower threat-score threshold <p>Run it directly or with the provided Docker setup:</p> <pre><code>cargo run -p tower-guard-advanced-app\n</code></pre> <pre><code>cd examples/advanced_app\ndocker compose up\n</code></pre>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#requirements","title":"Requirements","text":"<ul> <li>Rust 1.92 or later (the crate uses edition 2024)</li> <li>tower 0.5</li> </ul>"},{"location":"installation/#add-the-crate","title":"Add the crate","text":"<pre><code>cargo add tower-guard-rs\n</code></pre> <p>or add it to your <code>Cargo.toml</code> directly:</p> <pre><code>[dependencies]\ntower-guard-rs = \"1.0.0\"\ntower = \"0.5\"\n</code></pre> <p><code>tower-guard-rs</code> <code>1.0.0</code> depends on <code>guard-core-engine</code> <code>4.0.4</code>, the published detection engine crate. The adapter's version pin stays in lockstep with the published engine release.</p>"},{"location":"installation/#verify-the-installation","title":"Verify the installation","text":"<p>A minimal program that wraps a tiny service with the guard layer:</p> <pre><code>use bytes::Bytes;\nuse http::{Request, Response};\nuse http_body_util::Full;\nuse std::convert::Infallible;\nuse tower::{Layer, Service, ServiceExt};\n\n#[tokio::main]\nasync fn main() {\n    let upstream = tower::service_fn(|_request: Request&lt;Full&lt;Bytes&gt;&gt;| async {\n        Ok::&lt;_, Infallible&gt;(Response::new(Full::new(Bytes::from(\"hello\"))))\n    });\n\n    let mut service = tower_guard_rs::GuardLayer::new(tower_guard_rs::default_config())\n        .layer(upstream);\n\n    let request = Request::builder()\n        .uri(\"/hello\")\n        .body(Full::new(Bytes::new()))\n        .unwrap();\n    let response = service.ready().await.unwrap().call(request).await.unwrap();\n    assert_eq!(response.status(), 200);\n}\n</code></pre> <p>A request with a malicious query string, for example <code>/files/../../etc/passwd</code>, is blocked with <code>403 Forbidden</code> and a <code>{\"detail\":\"Suspicious activity detected\"}</code> body.</p>"},{"location":"installation/#building-from-source","title":"Building from source","text":"<p>The repository itself consumes the engine as a local path dependency on a sibling <code>guard-core-rs</code> checkout (see the repository README), so building the repository workspace locally requires that checkout to exist. Downstream applications that depend on the published crate are not affected: crates.io resolves <code>guard-core-engine</code> automatically.</p>"}]}