[workspace]
members = ["examples/simple_app", "examples/advanced_app"]

[package]
name = "tower-guard-rs"
version = "1.1.0"
edition = "2024"
rust-version = "1.92"
description = "Application-layer security middleware for tower-based services, powered by the guard-core-rs engine. Part of the guard ecosystem."
license = "MIT OR Apache-2.0"
repository = "https://github.com/rennf93/tower-guard-rs"
readme = "README.md"
keywords = ["security", "middleware", "api", "tower", "axum"]
categories = ["web-programming"]

[lib]
name = "tower_guard_rs"

[dependencies]
bytes = "1.10"
# guard-core-engine is published to crates.io (4.1.0); the path points at the
# engine crate inside a sibling guard-core-rs checkout so local builds and CI
# (see .github/workflows/ci.yml) exercise the engine from source. The version
# pin stays in lockstep with the published engine release.
guard-core-engine = { path = "../guard-core-rs/crates/guard-core-engine", version = "4.1.0" }
http = "1.3"
http-body = "1.0"
http-body-util = "0.1"
tower = { version = "0.5", default-features = false }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
tower = { version = "0.5", default-features = false, features = ["util"] }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all                     = { level = "deny", priority = -1 }
pedantic                = { level = "warn", priority = -1 }
missing_errors_doc      = "allow"
missing_panics_doc      = "allow"
module_name_repetitions = "allow"
